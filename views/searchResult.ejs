<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width">
  <title>Personal Library</title>
  <link href="/css/style.css" rel="stylesheet" type="text/css" />
</head>

<body>
  <nav>
    <a href="/books">
      View all books
    </a>
    <a href="/books/new">
      Add new books
    </a>
  </nav>
  <ul>
    <% books.forEach(book => 
      { %> <li> <h2><%= book.title %></h2> <p><strong>Author:</strong> <%= book.author %></p> <p><strong>Description:</strong> <%= book.description %></p> <% if (book.coverImage) 
{ %> 
      <img src="<%= book.coverImage %>" alt="Cover Image" width="100"> <% } else { %> <img src="/images/placeholder.png" alt="No Cover Image" width="100"> <% } %> 
        <a href="/books/edit/<%= book.id %>">Edit</a> 
        <a href="/books/delete/<%= book.id %>">Delete</a> 
      </li> <% }) %>
  </ul>
</body>

</html>